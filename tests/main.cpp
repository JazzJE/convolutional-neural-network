#include <iostream>

void run_matrix_tests();

int main()
{
    /* Tested relevant matrix operations (addition, subtraction, multiplication, scalar multiplication, equality, transpose) */
    run_matrix_tests();

    std::cout << "All tests passed!\n";

    return 0;
}